source 'http://rubygems.org'

fail "Ruby >=1.9.3 is required! Detected version: #{RUBY_VERSION}" unless RUBY_VERSION.to_s >= '1.9.3'

# This should automatically install nokogiri and other needed gems
gem('mechanize', '>= 2.7.5')
